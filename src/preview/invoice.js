/**
 * Creates a card with the preview of the invoice.
 * @param {String} text The text to overlay on the image.
 * @return {CardService.Card} The assembled card.
 */
function previewInvoice (obj) {
    return;
}